import { SortConfig, SortOrder } from "../../../types/api-response-types";

export const JOIN_REQUEST_SORT_CONFIG: SortConfig = {
  sortDirection: SortOrder.Descending,
  sortField: "SUBMITTED_DATE",
};
